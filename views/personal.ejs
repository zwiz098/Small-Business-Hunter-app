<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>SBS</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link rel="stylesheet" href="style.css">
  </head>
  <body>

    <div class="container">
    <!-- LOCAL INFORMATION -->
    <div class="col-sm-6">
        <div class="well">
            <h3><span class="fa fa-user"></span> <%= user.local.username %></h3>

            <% if (user.local.email) { %>
                <p>
                    <strong>id</strong>: <%= user._id %><br>
                    <strong>email</strong>: <%= user.local.email %><br>
                    <strong>password</strong>: <%= user.local.password %>
                </p>

                <a href="/unlink/local" class="btn btn-default">Unlink</a>
            <% } else { %>
                <a href="/connect/local" class="btn btn-default">Connect Local</a>
            <% } %>

        </div>
        <form class="messages" action="index.html" method="post">
          <input type="\button" name="" value="">
        </form>
        <h3>Messages</h3>
        <ul class="messages">
        <% for(var i=0; i<messages.length; i++) {%>
          <li class="message">
            <span><%= messages[i].typ %></span>
            <span><%= messages[i].bs %></span>
            <span><%= messages[i].thumbUp %></span>
            <span><i class="fa fa-thumbs-up" aria-hidden="true"></i></span>
            <span><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
            <span><i class="fa fa-trash" aria-hidden="true"></i></span>
          </li>
        <% } %>
        </ul>

        <h2>Add a message</h2>
      <% if (user.local.email) { %>
        <form action="/messages" method="POST" enctype="multipart/form-data">
          <input class="postImage" type="file" name="file-to-upload" placeholder="Upload Image" multiple>
          <input type="text" placeholder="Type of business" name="typ">
          <textarea name="bs" placeholder="Business description" rows="8" cols="80"></textarea>
          <button type="submit">Submit</button>
        </form>
      </div>
    </div>

  </div>

  <script src="main.js"></script>
  </body>
</html>
